name: ⚙️ Build Binaries

on:
  workflow_dispatch:
  workflow_call:

jobs:
  build-artifacts:
    uses: daylinmorgan/actions/.github/workflows/build-nim-binaries.yml@main
    with:
      module: todo

